/* @refresh reload */
import { render } from "solid-js/web";
const root = document.getElementById("root");
const Home = () => {
  return (
    <>
      <p>What's this project? - idk</p>
    </>
  );
};
render(() => <Home />, root!);
